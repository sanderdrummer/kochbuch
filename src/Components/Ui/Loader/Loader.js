import React from 'react';
import PropTypes from 'prop-types';

const Loader = (props) => {
    return null;
    // return props.ui.isLoading ? (<button className="is-primary loader button"></button>) : null;
};


Loader.propTypes = {};

export default Loader;